[build-system]
requires = [
    "setuptools >= 65",
    "wheel >= 0.38.1",
]
build-backend = 'setuptools.build_meta'

[tool.black]
line-length = 100
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.github
  | \.pytest_cache
  | \.venv
  | \venv
  | build
  | dist
  | docs
)/
'''
